# Decision log

Accepted architecture decisions live in `docs/adr/`. Use this file for small operational choices that do not merit an ADR.

| Date | Milestone | Decision | Reason | Reversible? | Reference |
|---|---|---|---|---|---|
| 2026-08-06 | Bootstrap | Autonomous implementation stops after M4 | Owner review before evidence/forecasting expansion | Yes | ADR-0007 |
| 2026-08-06 | M0 | Schema version is a `ClassVar` on `VersionedModel`, re-attached by `to_record()` | Version describes the class, not a mutable field; a subclass that does not declare its **own** version fails at import time (checked via `cls.__dict__`, not `getattr`, so a subclass cannot inherit and mislabel its records) | Yes | `src/argos/domain/versioning.py` |
| 2026-08-06 | M0 | Container fields on `VersionedModel` are deep-frozen on validation | `frozen=True` blocks only attribute assignment; a `dict` field stays mutable and the mutation reaches `to_record()` — invariant 7 needs the object itself to be unable to lie | Yes | `freeze`/`thaw` in `src/argos/domain/versioning.py` |
| 2026-08-06 | M0 | `RunManifest.created_at` is normalized by a field validator, not only by `build_run_manifest` | A manifest read back from disk must be as trustworthy as one just built, and the same instant must always serialize identically or M3 hashes diverge | Yes | `src/argos/config/manifest.py` |
| 2026-08-06 | M0 | `InvalidDurationError` subclasses both `ClockError` and `ValueError` | `nan` slipped past the `seconds < 0` guard and would hang `anyio.sleep`; the failure needs a countable code while staying catchable by stdlib-shaped callers | Yes | `src/argos/clock/base.py` |
| 2026-08-06 | M0 | Configuration errors report field names and error types only, never the offending value | pydantic embeds `input_value` in its message; an operator who pastes a credential into the wrong `ARGOS_*` variable must not see it echoed into stderr or CI logs | Yes | `docs/09_SECURITY.md` |
| 2026-08-06 | M0 | `_code_revision` verifies the git toplevel before trusting `HEAD` | git discovers repositories upward, so a wheel install inside an unrelated checkout would stamp manifests with foreign provenance — silently, since git succeeds | Yes | `src/argos/cli.py` |
| 2026-08-06 | M0 | Unrecognized `ARGOS_*` environment variables fail the load | pydantic-settings ignores them, so a typo would silently keep a default and a stray credential-shaped variable would go unnoticed | Yes | `src/argos/config/settings.py` |
| 2026-08-06 | M0 | `ensure_utc` rejects naive datetimes instead of assuming UTC | A naive timestamp is ambiguous evidence; assuming UTC would corrupt event-time reasoning | Yes | `src/argos/clock/base.py` |
| 2026-08-06 | M0 | `ReplayClock.advance_to` accepts an equal instant but rejects backwards moves | Several observations legitimately share one event time; regression is a determinism bug | Yes | `tests/test_clock.py` |
| 2026-08-06 | M0 | Architecture and no-execution boundaries enforced by AST tests, not review alone | A rule that only exists in prose drifts; `tests/test_boundaries.py` fails the build instead | Yes | `tests/test_boundaries.py` |
| 2026-08-07 | M1 | `MarketDefinitionV1` validates structure only; binary-ness is a selection policy | A three-outcome market is not malformed, it is out of scope — conflating the two would hide real normalization failures behind a scope filter | Yes | `src/argos/domain/selection.py` |
| 2026-08-07 | M1 | A timeout that exhausts the retry budget raises `SourceTimeoutError`, an outage raises `SourceUnavailableError` | They are different operational stories and lead to different fixes | Yes | `src/argos/sources/gamma.py` |
| 2026-08-07 | M1 | ~~Retry backoff sleeps on the injected clock~~ **superseded 2026-08-07** | Original reason: tests and replay must not spend real seconds, and retry *timing* is excluded from any output hash. That holds only while no output hash exists. Under the replay scheduler's clock, `ReplayClock.sleep` advances virtual time by an unseeded-jitter amount, which would break M3's identical-hash criterion. Resolved by ADR-0009 | Yes | ADR-0009 |
| 2026-08-07 | pre-M2 | `Clock` keeps `now()` only; a new `Pacer` port (`wait`, `move_on_after`; `RealPacer` on `anyio`) owns all real elapsed time, and retry jitter is drawn from an adapter-owned, seeded `random.Random` | Merging timekeeping and pacing on one protocol produced four live defects: a backoff sleep replayed on `ReplayClock` invents or double-counts events that were never captured; `wait_exponential_jitter` draws from an unseeded module-global RNG, making backoff depend on sibling processing order (order-dependent hidden global state, prohibited outright); `ReplayClock.sleep` contained no `await`, so a cancellation aimed at it could never be delivered; and `GammaClient._get`'s `anyio.move_on_after` deadline read the event loop's monotonic clock, bypassing the injected `Clock` entirely — reproduced with five virtual hours of `ReplayClock.sleep` inside a 0.05 s deadline scope not firing | Yes | ADR-0009 |
| 2026-08-07 | pre-M2 | `RunManifest` is `run_manifest.v2`, a breaking change with no migration | `mode` becomes a `RunMode` enum, `working_tree` (`WorkingTreeStatus`) joins `code_revision` so a dirty tree can no longer misattribute a run's code, and `schema_versions`/`input_provenance` close core invariant 13 in full. No `v1` manifest was ever persisted, so a migration would be a compatibility wrapper for a case that never occurred | Yes | `src/argos/config/manifest.py` |
| 2026-08-07 | M1 | `contract_id` is derived from the market's own rule-bearing content, not from `raw_payload_sha256` | That field hashes the whole response *page*, so every market in a page shared it and the same market fetched by id had a different one — one market, two identities, and a new one whenever an unrelated sibling's volume ticked | Yes | `src/argos/compiler/contract.py` |
| 2026-08-07 | M1 | `subject_entities` and `qualifying_event` are present but empty, like the conditions | They are equally un-extractable before the owner gate; omitting them from the schema instead would make their absence invisible and force a v2 for a record that was never complete | Yes | `docs/04_DATA_CONTRACTS.md` |
| 2026-08-07 | M1 | The response body is streamed with an incremental size check | Reading `.content` first buffers *and decompresses* the whole body, so the cap reported memory exhaustion rather than preventing it — a 611 KB gzip body reached 1.2 GiB of RSS in review | Yes | `src/argos/sources/gamma.py` |
| 2026-08-07 | M1 | One overall deadline bounds a request, retries included | httpx's read timeout is per chunk, so a server dripping a byte at a time kept a request alive indefinitely and the retry budget never applied | Yes | `docs/09_SECURITY.md` |
| 2026-08-07 | M1 | Third-party text is control-character-sanitized and always block-quoted before rendering | A newline in the question forged whole sections of the audit — including a `review status: human_reviewed` line — and OSC 52 in a description writes to the reviewer's clipboard. The artifact a human reviews must not be authored by the source (invariant 15) | Yes | `src/argos/compiler/audit.py` |
| 2026-08-07 | M1 | `MACHINE_CHECKED` is not computed at compiler v1 | `CONDITIONS_NOT_EXTRACTED` is unconditional, so a branch on it was dead code pretending to be a decision | Yes | `src/argos/compiler/contract.py` |
| 2026-08-07 | M1 | Token ids are compared by integer value for duplicate detection | `"007"` and `"7"` are the same uint256 token, so a string comparison let both sides of a market map to one token | Yes | `src/argos/domain/market.py` |
| 2026-08-07 | M1 | `http://` and `ws://` are no longer accepted endpoint schemes | `docs/09_SECURITY.md` requires it, and M1 is the first milestone that opens a socket | Yes | `src/argos/config/settings.py` |
| 2026-08-07 | M1 | `CONDITIONS_NOT_EXTRACTED` is always flagged at compiler v1 | `yes_condition`/`no_condition` need semantic extraction, which is gated behind the owner review; a flag is honest where a guess would silently redefine resolution (invariant 15) | Yes | ADR-0005 |
| 2026-08-07 | M1 | `ambiguity_score` is an integer count of flags | Invariant 8 forbids an uncalibrated score that could be read as a probability; a count above 1 cannot be | Yes | `docs/04_DATA_CONTRACTS.md` |
| 2026-08-07 | M1 | A missing liquidity value is treated as zero against a floor, not as "unknown, pass" | Absent evidence must not let a market through a filter it was never measured against | Yes | `src/argos/domain/selection.py` |
| 2026-08-07 | M1 | `--min-liquidity` is parsed as a string into `Decimal` | A float CLI option would round money before it reached the policy | Yes | `src/argos/cli.py` |
| 2026-08-06 | M0 | Storage engine choice deferred to M2 | The SQLite/WAL vs append-log comparison needs real capture volumes; deciding now would be speculation | Yes | `docs/12_TECH_STACK.md` |
| 2026-08-10 | M2 | `observation_id` combines every stable discriminator rather than ranking one above the rest | An earlier draft ranked source `hash` above source `sequence` above payload content. Against the real CLOB book, a book moving A -> B -> A returns `hash(A)` on both the first and third poll, so ranking the hash would have collided the third observation with the first and silently erased the revert; a source sequence that resets after reconnect collides two different events the same way | Yes | ADR-0010 |
| 2026-08-10 | M2 | Identity fields are length-prefixed before hashing instead of joined on a separator | Joining on `\x1f` was not injective: `source_event_type="book\x1fmarket-1"` with no `market_id` produced identity-colliding material with `source_event_type="book"` and `market_id="market-1\x1f"`. Reachable from any source-controlled string field, and reproduced on the `market_id`/`condition_id` and `condition_id`/`token_id` boundaries too | Yes | `src/argos/domain/observation.py::_digest` |
| 2026-08-10 | M2 | `ObservationEnvelopeV1.payload` stores the payload's canonical JSON mapping, with typed access via `read_payload` | The envelope previously held live Python objects; `to_record()` dumps in JSON mode, so `Decimal('0.5')` reloaded as the string `'0.5'` and the replayed envelope differed from the live one while keeping the same `observation_id` — invariant 5 broken, and M3's identical-hash criterion runs through this object | Yes | ADR-0010 |
| 2026-08-10 | M2 | `build_observation_envelope` takes the typed `VersionedModel` payload, not a mapping plus a version string | A caller could previously label any mapping with any `payload_schema_version`; the mismatch would surface only when `read_payload` ran during replay, against a capture that can no longer be re-taken | Yes | `src/argos/domain/observation.py::build_observation_envelope` |
| 2026-08-10 | M2 | `SourceProvenanceV1.http_status` is nullable | Mandatory made it impossible for a same-milestone WebSocket adapter to populate provenance without inventing an HTTP status inside a transport-agnostic contract | Yes | `src/argos/domain/provenance.py` |
| 2026-08-10 | M2 | `VersionedModel.model_copy` re-validates instead of inheriting pydantic's default `model_copy(update=...)` | pydantic's default does not re-validate, so a bare mutable dict could land in a stored payload and a later external mutation would reach `to_record()` | Yes | `src/argos/domain/versioning.py` |
| 2026-08-10 | M2 | The control-character sanitizer moved to a shared `argos.domain.text` module used by both `compiler.audit` and `domain.observation` | An earlier justification for duplicating it into the domain ("domain may not import compiler") was false — the legal direction is compiler-to-domain, and its docstring claimed a parity test that did not exist. Two divergent copies of a security-relevant sanitizer with no parity test is the drift the M1 security review exists because of | Yes | `src/argos/domain/text.py` |
| 2026-08-10 | M2 | Unicode tag characters (U+E0000-U+E007F) and U+200B/U+FEFF are neutralized; ZWJ/ZWNJ are deliberately exempted | Adversarial testing found these are Unicode category `Cf`, not `Cc`, so they survived the original sanitizer as an invisible-text smuggling channel. ZWJ/ZWNJ are load-bearing in Indic and Perso-Arabic scripts and in emoji sequences and cannot reorder or hide surrounding text, so neutralizing them would corrupt legitimate text to close a channel they do not open | Yes | `src/argos/domain/text.py::is_display_control` |
| 2026-08-11 | M2 | SQLite/WAL chosen over an append log | Both candidates clear the measured ~1-2/s volume requirement by roughly four orders of magnitude, so volume did not decide it. "Idempotent and observable" (`docs/02_ARCHITECTURE.md`) is a two-write operation that must be atomic; SQLite does it in one transaction, an append log needs a journal to close the crash window, which is re-implementing SQLite worse | Yes | ADR-0011 |
| 2026-08-11 | M2 | The `observation` table is a rowid table with `UNIQUE(observation_id)`, not `WITHOUT ROWID` | Measured and independently reproduced: a >1 KB record spills to overflow pages inside a `WITHOUT ROWID` index B-tree, costing 4,681 B/row against 1,456 B/row | Yes | ADR-0011, section 2 |
| 2026-08-11 | M2 | Parquet is rejected for M2 outright | New large dependency, columnar and wrong for row-at-a-time append and sequential replay, and enforces no unique key at all. Reconsiderable only as an M4 export format | Yes | ADR-0011, section 2 |
| 2026-08-11 | M2 | `delivery` is one row per arrival, not a duplicate counter | A counter discards `received_time`/`ingest_sequence` for every arrival after the first, which core invariant 6 and ADR-0003 require to be preserved separately | Yes | ADR-0011, section 5 |
| 2026-08-11 | M2 | `capture_run` is append-only; closing a run inserts a second row rather than updating a column | Resolves a contradiction found during implementation: ADR-0011 as first drafted described closing a run as setting `ended_at` on an existing row, which is itself a mutation and violates the ADR's own "no `UPDATE` in `argos.store`" consequence. Cost accepted knowingly: `capture_run_id` is no longer unique in that table, so the referential check moves into Python and is weaker than a database foreign key | Yes | ADR-0011, section 5, dated Correction 2026-08-11 |
| 2026-08-11 | M2 | `rejection_id` is a non-unique grouping key; the `rejection` table keys on `(capture_run_id, ingest_sequence)` | `_rejection_identity` has no within-payload discriminator, so two different malformed entries in one frame, refused for the same reason against the same archived frame hash, would otherwise collapse onto one `rejection_id` and silently lose one. Keying the table on arrival instead preserves both rows | Yes | ADR-0011, section 7 |
| 2026-08-11 | M2 | The raw archive survives rather than being absorbed into the event store | A multi-MiB REST body has no business in a row; the store references archived bytes by `raw_payload_sha256`/`raw_payload_location`, which the envelope already carries, keeping rows at the measured ~1.4 KB | Yes | ADR-0011, section 4 |
| 2026-08-11 | M2 | `is_clean_identifier` refuses `\n`/`\t`/`\r` independently of `is_display_control` | `is_display_control` deliberately exempts `\n`/`\t` because they are correct for prose (where the M1 defence is sanitizer plus block-quoting). Delegating identifier validation wholly to it meant a newline passed identifier validation on `market_id`/`condition_id`/`token_id`/`source_sequence`/`source_hash`/`capture_run_id` through two security reviews — a newline is the original M1 attack that forged `review status: human_reviewed` into an audit | Yes | `src/argos/domain/text.py::is_clean_identifier` |
| 2026-08-11 | M2 | A CLOB `/book` 404 is recorded as an ambiguous, counted outcome (`ClobBookNotFoundError`, `ClobHealth.not_found`) and never read as "market closed" | A closed market, an unknown token id, and a syntactically valid token id that never had a book are indistinguishable from the response alone (`docs/research/m2-clob-rest-book.md`); lifecycle determination belongs to the M1 Gamma metadata, not to a status-code inference in this adapter | Yes | `src/argos/sources/clob.py::ClobBookNotFoundError` |
| 2026-08-11 | M2 | `ClobHealth.not_found`/`bad_request` are counted separately from `failures` | A 404 is an expected, ambiguous outcome on this source, not evidence the adapter itself is unhealthy; folding it into `failures` would make an ordinary capture session look degraded | Yes | `src/argos/sources/clob.py::ClobHealth` |
| 2026-08-11 | M2 | The response `asset_id` is cross-checked against the requested `token_id` in `normalize_clob_book` | A proxy or cache serving the wrong token's book would otherwise silently attribute one market's order book to another, and the substitution would be unfalsifiable after the fact | Yes | `src/argos/ingestion/clob_book.py::normalize_clob_book` |
| 2026-08-11 | M2 | `normalize_clob_book` refuses a response above `MAX_NORMALIZABLE_BYTES` with a rejection record rather than a raise | Security review measured a 31 MiB `timestamp` string costing 21.18 s CPU and being *accepted*, and a `Pacer` deadline cannot bound synchronous CPU work (a cancel scope cannot interrupt it, measured at 14.21 s elapsed against a 0.50 s deadline). The bytes really did arrive, so a refusal that leaves no ledger entry would itself be the silent drop core invariant 14 forbids | Yes | `src/argos/ingestion/clob_book.py::MAX_NORMALIZABLE_BYTES` |
| 2026-08-11 | M2 | `_normalize_decimal` collapses negative zero (`Decimal("-0")`) onto positive zero | `Decimal('-0') >= Decimal('0')` is `True`, so it passed the `ge=MIN_PRICE` validator on `last_trade_price` and a level's `price` while rendering as the distinct canonical text `"-0"`; since identity hashes rendered text, one economic price spelled two ways minted two `observation_id`s — found by adversarial testing, the second independent instance of the ADR-0010 decimal-normalization class in this function | Yes | `src/argos/domain/orderbook.py::_normalize_decimal` |
| 2026-08-11 | M2 | `ingest_sequence` is a caller-supplied parameter to `normalize_clob_book`, not allocated by it | The WebSocket research found a frame can carry entries for an unsubscribed sibling token; assigning sequence numbers before subscription filtering would make `ingest_sequence` depend on connection topology, and M3 replay determinism would inherit that. `normalize_clob_book` is a pure, synchronous normalization step, not the capture loop, and allocation is left for that later slice to decide deliberately | Yes | `src/argos/ingestion/clob_book.py::normalize_clob_book` |
| 2026-08-12 | M2 | `PriceChangeV1` carries the level changes themselves, not only the group's `(timestamp, hash)` | Measured on the recorded live capture: the same `(timestamp, hash)` pair for the **same token** arrives across up to **three separate WebSocket frames**, each carrying a *different* price level change (token `34691…637961`, timestamp `1786387666174`, hash `5ce704de…`: `0.49/636`, then `0.65/142.85`, then `0.48/17`). ADR-0010 identity hashes the canonical payload, so a payload holding only the hash would collapse three genuine deltas onto one `observation_id` and silently lose two. This makes ADR-0010's documented residual collision reachable in practice, and the payload contents are what keep it closed | Yes | `src/argos/domain/pricechange.py`, `tests/test_price_change.py` |
| 2026-08-12 | M2 | A zero-size `price_change` entry is `PriceLevelChangeKind.REMOVE`, **not** an `OrderBookAnomaly` | Confirmed on live traffic, not inferred from documentation: on the delta stream, size `"0"` is the source's only vocabulary for removal, so it is normal semantics. In a *full REST snapshot* the same value is a defect (`ZERO_SIZE_LEVEL_DROPPED`), because a snapshot describing a resting order of size zero is malformed. The two conventions are deliberately different and the difference is the point | Yes | `docs/research/m2-clob-websocket.md`, `src/argos/domain/pricechange.py` |
| 2026-08-12 | M2 | `kind` is validated against `size` rather than derived silently on read | A directly-constructed or replayed `PriceLevelChangeV1` claiming `SET` at size 0, or `REMOVE` at nonzero size, is refused. Re-deriving instead would let a stored record disagree with its own meaning and be quietly corrected on the way out — the same "recomputable, never drifting" discipline `_validate_anomalies_are_recomputable` already applies | Yes | `src/argos/domain/pricechange.py::PriceLevelChangeV1` |
| 2026-08-12 | M2 | Every identity-bearing decimal operation runs inside a pinned `CANONICAL_DECIMAL_CONTEXT`, and out-of-budget magnitudes are refused rather than accommodated by a wider context | `normalize()`, `quantize()` and `%` all read `decimal.getcontext()`, thread-local mutable global state: one wire price rendered as three different canonical texts — three different `observation_id`s — under ambient precisions 5/28/50. That is the hidden global state CLAUDE.md prohibits and a direct break of core invariant 5, since replay under a different ambient context would not reproduce live identity. Widening the context instead would be a fresh resource-exhaustion vector: `"1E+1000000"` is 11 wire bytes that would render as a one-megabyte canonical integer | Yes | `src/argos/domain/orderbook.py::CANONICAL_DECIMAL_CONTEXT` |
| 2026-08-12 | M2 | The exponent budget is one-sided (positive only), the significant-digit budget is not | Only a positive exponent reaches the quantize branch that expands a value into plain integer text, which is the path where a short wire value becomes an unboundedly long canonical string; a very negative exponent keeps a short scientific rendering that is just as deterministic. A symmetric bound looked tidier and silently narrowed an already-shipped contract — caught by the committed test that pins a `1E-50` tick size as preserved | Yes | `src/argos/domain/orderbook.py::MAX_DECIMAL_EXPONENT` |
| 2026-08-12 | M2 | An oversized `int` is refused by `bit_length()` before `Decimal(int)` conversion | Converting a huge Python `int` to `Decimal` is superlinear: measured at **63.12 s** of CPU for a 1,000,001-digit int, which no `Pacer` can bound. The magnitude budget would have refused the value, but only *after* paying that cost — the recurring "the bound runs downstream of the work it is meant to bound" shape. Not reachable through this project's own JSON decoding today, because CPython's `int_max_str_digits` default of 4300 blocks it, which is a third-party default this module does not own and must not silently depend on. Refusal now costs 0.21 s | Yes | `src/argos/domain/orderbook.py::_refuse_oversized_int` |
| 2026-08-12 | M2 | An unrecognized **top-level** `price_change` key refuses the frame, matching the existing entry-level check | Found by independent adversarial testing: entry keys were checked and top-level keys were not, so an unrecognized top-level key vanished with no count and no reason. The key that matters is `sequence`: this channel is confirmed by direct observation to have none, which is why gap detection is built on `(timestamp, hash)` reconciliation — if the source ever started sending one, the old behaviour would have discarded it silently while ARGOS went on reconciling the hard way. Cost of the fix: a source schema change halts ingestion loudly, which is the same trade already made one level down | Yes | `src/argos/domain/pricechange.py::EXPECTED_EVENT_KEYS` |
| 2026-08-12 | M2 | An entry that is not an object, or carries no string `asset_id`, refuses the frame instead of being skipped | `parse_price_change_group` is called once per token, so an unattributable entry would have been discarded for *every* token — never counted, never reasoned. Refusing costs one counted rejection per token in the ledger, which is the visibility invariant 14 asks for, and matches `_build_level`'s treatment of a non-object book level | Yes | `src/argos/domain/pricechange.py::parse_price_change_group` |
| 2026-08-13 | M2 | `normalize_clob_price_change` returns `None` for a frame carrying no entry for the requested token, rather than a rejection | The research note established by direct observation that subscribing to one token delivers `price_change` entries for its unsubscribed binary sibling, so a frame naming only the sibling is normal, healthy traffic about a token nobody asked this call about. Writing a ledger row for each would flood the rejection ledger with rows describing healthy traffic and destroy its signal value. `None` is a **counted non-event**, not a silent drop: the capture loop must count it, and the docstring says so unmissably, because this function has no health-counter to write to and no ledger row is the honest representation of "this frame was never about this token" | Yes | `src/argos/ingestion/clob_price_change.py` |
| 2026-08-13 | M2 | That case is signalled by a named `NoEntriesForToken(ValueError)` rather than by matching the exception's message text | The ingestion layer first recognised it by comparing `str(error)` against a literal copied from the domain module's source. That coupling was safe in its failure direction — a text change yields a noisy false rejection rather than a silent drop — but it rots invisibly. A named type moves the contract into the type system, where a rename breaks the import instead of quietly changing behaviour. It subclasses `ValueError` so every other consumer keeps working with a single `except ValueError` | Yes | `src/argos/domain/pricechange.py::NoEntriesForToken` |
| 2026-08-13 | M2 | The millisecond wire-timestamp parser moves to a shared `argos.ingestion.wire`, used by both the REST and WebSocket adapters | Both sources carry an identical `timestamp` string, and duplicated normalization logic is a defect class this repository has already been bitten by twice with `Decimal` scale. One copy cannot silently drift from the other. The move is behaviour-preserving; the REST path's three-way PRESENT/UNPARSEABLE/MISSING distinction is unchanged | Yes | `src/argos/ingestion/wire.py` |
| 2026-08-13 | M2 | `source_event_type` is read from the wire for the WebSocket frame (`"price_change"`), not assigned by ARGOS | The REST `/book` response carries no `event_type`, so `clob_book.py` had to invent `"book_snapshot"`. The WebSocket frame carries its own, so inventing a second label for the same concept would only create two names that can disagree | Yes | `src/argos/ingestion/clob_price_change.py` |
| 2026-08-13 | M2 | `normalize_decimal` enforces a fail-closed equality postcondition in addition to its magnitude bounds | The bounds are a fast, legible refusal; the postcondition is what actually guarantees "canonicalization never silently changes a finite non-zero value", without depending on anyone having reasoned correctly about `Emin`, `prec`, or which branch expands which exponent. That dependence is exactly how the underflow-to-zero defect survived review: the previous slice removed the lower exponent bound on a *text-length* argument, and the defect was *arithmetic*. Reproduced: `parse_wire_decimal("1E-1000064")` returned `Decimal(0)`, sharing a genuine zero's canonical text and therefore its identity | Yes | `src/argos/domain/orderbook.py::normalize_decimal` |
| 2026-08-13 | M2 | `CANONICAL_DECIMAL_CONTEXT` sets `Emin`/`Emax` explicitly rather than inheriting them | An unspecified `Context` field is filled from `decimal.DefaultContext`, which is mutable process-global state — the same class of dependency the pinned context exists to remove, merely moved from read time to import time. `Emin` is also precisely what the underflow above turned on | Yes | `src/argos/domain/orderbook.py::CANONICAL_DECIMAL_CONTEXT` |
| 2026-08-13 | M2 | Every selected `hash` is validated as a non-empty string before the hash set is built or sorted | An unhashable value (`{"hash": []}`) raised `TypeError: unhashable type` from the set comprehension, and a frame mixing `"a"` with `3` raised `TypeError: '<' not supported` from the `sorted()` call that builds a *different* refusal's error message. A `TypeError` is outside the ARGOS taxonomy, so both flew past the ingestion layer's `except ValueError` and left no rejection-ledger entry — the third distinct instance of that shape in M2. The message uses a bounded `repr` so composing a rejection detail cannot itself become the exhaustion vector | Yes | `src/argos/domain/pricechange.py::parse_price_change_group` |
| 2026-08-14 | M2 | `is_display_control` treats Unicode category `Cs` (lone surrogates) as neutralizable | Not a display problem but an encodability one: `str.encode()` is strict UTF-8 and raises on an unpaired surrogate, while `json` decodes `"\ud800"` into exactly that string without checking pairing. `is_clean_identifier` therefore called it clean, it passed every validator, and `_digest` raised `UnicodeEncodeError` outside every ingestion `try` — no ledger row, on both adapters. Handling it in this one predicate closes both paths at once: identifiers refuse it, prose replaces it, which is what keeps a rejection record writable while describing the very input that cannot be encoded | Yes | `src/argos/domain/text.py::is_display_control` |
| 2026-08-14 | M2 | Identifiers on the rejection ledger are neutralized with the identifier rule, not the prose rule (`neutralize_identifier_and_bound`) | Prose deliberately exempts newline and tab, correctly — a market description contains them and the M1 defence there is sanitizer plus block-quoting. An identifier has no such exemption, and reusing the prose rule let a newline in `market` survive into a stored `condition_id` carrying `review status: human_reviewed`: the original M1 audit-forgery attack, on a durable field, waiting for the first renderer that does not block-quote | Yes | `src/argos/domain/text.py::neutralize_identifier_and_bound` |
| 2026-08-14 | M2 | No format is enforced on the source's `hash`, so a non-hex spelling is accepted | The research observed 40 hex characters on every real sample but never established it as a source guarantee, and ARGOS only passes the value through as `source_hash`. Enforcing 40-hex would convert a legitimate future format change into a total rejection storm. Contrast `token_id`, where `[0-9]{1,120}` *is* enforced because the research did establish it: enforce what the evidence supports, not what looks tidy | Yes | `tests/test_clob_price_change_ingestion_adversarial.py` |
| 2026-08-14 | M2 | The WebSocket transport yields raw frames and decodes nothing | Core invariant 7: raw data is immutable and normalization is a separate versioned step, which already exists in `argos.ingestion`. A transport that parsed JSON, split arrays, or filtered by token would be making normalization decisions with no schema version attached to them | Yes | `src/argos/sources/clob_ws.py` |
| 2026-08-14 | M2 | The frame buffer is bounded at 64 and blocks when full, rather than dropping frames | A stalled consumer must apply real backpressure: once the buffer fills, `recv()` is not called again and backpressure reaches the TCP receive buffer. Dropping to keep up would be an uncounted loss. The heartbeat task is independent so a slow consumer does not look like a dead connection to the source | Yes | `src/argos/sources/clob_ws.py` |
| 2026-08-14 | M2 | `ingest_sequence` is allocated only when a record is actually written ("peek, then commit"), never for a candidate that produces nothing | A `None` from the normalizer means the frame was not about that token — a counted non-event, not an ingested one. Reserving a number unconditionally and skipping the write would carve permanent gaps into the ledger for every unsubscribed-sibling frame, which the research shows is routine traffic on this source rather than an edge case | Yes | `src/argos/ingestion/capture.py` |
| 2026-08-14 | M2 | Cross-token fan-out iterates the *configured* subscribed token set, sorted, never the tokens a frame happens to mention | The load-bearing decision for M3. One frame carries entries for the unsubscribed binary sibling, so iterating frame membership would make `ingest_sequence` a function of what the server bundled — connection topology — and replay determinism would inherit it. Verified: identical sequences with and without the sibling subscribed | Yes | `src/argos/ingestion/capture.py` |
| 2026-08-14 | M2 | The capture manifest is `RunManifest` plus the store's append-only `capture_run` rows, not a new concept | Those already carry run identity, open/close state, and derived counts. `run_capture` writes no `SourceProvenanceV1` of its own — every one it touches is already inside a stored envelope or rejection — which honours "an M2 capture manifest must not embed one provenance record per ingested event" by construction rather than by added logic | Yes | `src/argos/ingestion/capture.py` |
| 2026-08-14 | M2 | An event type with no payload model wired (`book`, `last_trade_price`, `tick_size_change`) is written as an `UNKNOWN_EVENT_TYPE` rejection, not skipped | Invariant 14 wants it visible rather than convenient. The four `book` events in the recorded capture become four counted rejections; inventing a normalizer for them in this slice would have been scope drift, and dropping them would have been a silent loss | Yes | `src/argos/ingestion/capture.py` |
| 2026-08-14 | M2 | The transport's oversized-frame counter is the honest maximum; no ledger row is written | The library refuses the frame during reassembly, so no bytes and no `raw_payload_sha256` ever reach ARGOS, and that field is required. A row would fabricate a hash for content never received. This corrects a backlog item I had written asserting the capture loop must close the gap | Yes | `docs/BACKLOG.md`, `src/argos/sources/clob_ws.py` |
