# Git workflow rule

- One vertical slice per small local commit.
- Inspect `git diff` before staging.
- Do not force push, hard reset, clean destructively, rewrite history, or delete owner data.
- Do not mix generated capture data with source commits.
- Update STATUS and BACKLOG in the same slice that changes milestone reality.
